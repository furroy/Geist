# Geist Engine Configuration Template

# Application name (displayed in window title)
name = Geist Example

# Display settings
h_res = 1280                      # Screen width
v_res = 720                       # Screen height
h_renderres = 640                 # Render target width
v_renderres = 360                 # Render target height
bpp = 32                          # Bits per pixel
full_screen = 0                   # 0 = windowed, 1 = fullscreen

# Performance
milliseconds_between_updates = 16.6666  # ~60 FPS
